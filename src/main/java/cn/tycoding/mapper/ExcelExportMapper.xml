<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.tycoding.mapper.IExcelExportDAO">

    <select id="export" parameterType="Integer" resultType="java.util.HashMap">
        select * from logs1 LIMIT #{limit}
    </select>

</mapper>